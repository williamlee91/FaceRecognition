1、此次版本三由于自己的笔记本只有2G显存，所以主要采用轻量级网络Squeezenet，参数为70多万个

2、在特征级联时应该设置为-1，而不是1，否则特征最后无法提取

3、跑网络过程中需要指定特定的GPU以及所需占用的显存大小，否则会溢出

4、即使采用轻量化网络，全部训练完也需要400多分钟

5、识别效果有很大提升，阈值设置时也可以设置的比较高

6、但是针对一些数据集识别错误的概率依旧比较高

7、下个版本打算采用VGG16
